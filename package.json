{
  "name": "gdl-games",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo of Nexus Mods Vortex game extensions, built from a single shared GDL toolchain.",
  "license": "GPL-3.0-or-later",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "init-gdl": "cd gdl && pnpm install && pnpm build",
    "build": "pnpm -r --filter \"./games/*\" build",
    "test": "pnpm -r --filter \"./games/*\" test",
    "test:corpus": "pnpm -r --filter \"./games/*\" test:corpus",
    "package": "pnpm -r --filter \"./games/*\" package"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
